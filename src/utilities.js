export default {
  /**
   * Detect if two objects are on-screen and colliding
   * @param {GameObject} objectONe Object
   * @param {GameObject} ObjectTwo Object
   * @param {Boolean} onScreenOnly Detect only if objects are onscreen
   * @returns {Boolean} Returns false if collision is detected (or if onScreenOnly is true and any of the object is offscreen) and false otherwsie
   */
  isColliding(objectOne, ObjectTwo, onScreenOnly) {
    // TODO: implement collision detection
  },
}
